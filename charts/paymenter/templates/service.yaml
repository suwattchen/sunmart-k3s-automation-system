apiVersion: v1
kind: Service
metadata:
  name: {{ include "paymenter.fullname" . }}
  labels:
    {{- include "paymenter.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paymenter.selectorLabels" . | nindent 4 }}
